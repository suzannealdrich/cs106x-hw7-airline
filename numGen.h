#ifndef _numGen_h_#define _numGen_h_/* Function: GenerateConfirmationNumber * Usage:	 confirmationNumber = GenerateConfirmationNumber(); * ------------------------------------------------------------ * This function produces a confirmation number similar to those given * during commercial airline ticket sales.  The confirmation number * includes both numerical digits and letters.  Hence, it is returned * as a string.  To achieve best results with this function, Randomize * should be called before calling it. */string GenerateConfirmationNumber (void);#endif